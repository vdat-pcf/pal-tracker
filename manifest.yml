applications:
  - name: pal-tracker
    path: build/libs/pal-tracker.jar
    random-route: pal-tracker-vijaya-datla.apps.evans.pal.pivotal.io
    env:
      WELCOME_MESSAGE: Hello from review environment2
      JBP_CONFIG_OPEN_JDK_JRE: '{ jre: { version: 11.+ } }'

    services:
      - tracker-database